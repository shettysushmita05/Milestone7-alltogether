import React from "react";

const UserDashboard = () => (
  <div className="dashboard user">
    <h2>User Dashboard</h2>
    <p>Welcome, User! You can view your data and profile.</p>
  </div>
);

export default UserDashboard;
